<div class='container'>
  <%= render partial: 'shared/trip_itinerary_nav' %>

  <section class='my-2'>
    <div class='p-3 m-2 bg-body-tertiary'>
      <h4 class='card-title mb-3'>Add an Expense</h4>
      <%= render 'form' %>
    </div>
  </section>
</div>
