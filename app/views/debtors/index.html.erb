<div class='container pb-2 mt-3' style="max-width: 720px;">
  <%= render partial: 'shared/trip_itinerary_nav' %>

  <section class='my-2'>
    <div class='p-3 bg-light card rounded-4 table-responsive'>
      <h4 class='display-5 mb-3'>Debts</h4>
      <% if @user_debts.present? %>
        <%= render partial: 'table', locals: { debts: @user_debts } %>
      <% else %>
        <p>No outstanding debts.</p>
      <% end %>
    </div>
  </section>
</div>


